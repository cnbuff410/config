# Multiple links share same tunnel
ControlMaster auto
ControlPath /tmp/ssh_mux_%h_%p_%r

# Persistent connection(for server)
ControlPersist 10h

# Keep alive for a selection of hosts
Host car.colorado.edu 54.242.183.238
ServerAliveInterval 10
